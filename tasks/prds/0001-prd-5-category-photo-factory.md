# PRD-0001: 5-Category 포토 팩토리 자동화 시스템

**작성일**: 2025-01-12
**상태**: Draft
**버전**: 1.0.0
**작성자**: Claude AI
**우선순위**: High

---

## 1. 문제 정의 (Problem Statement)

### 현재 상황
- 현장 기술자가 작업 후 마케팅용 콘텐츠 생산에 시간과 노력을 추가로 투입해야 함
- 불규칙한 촬영 방식으로 인해 콘텐츠 품질이 일정하지 않음
- 마케터가 각 플랫폼별 콘텐츠를 처음부터 제작하는 데 과도한 시간 소요
- 지역 기반 SEO 전략(네이버 검색) 미흡으로 신규 고객 유입 제한

### 해결할 문제
**"현장 촬영 2분 + 마케터 가공 10분 = 3개 플랫폼 콘텐츠 자동 생성"**

현장 기술자의 추가 부담 없이 표준화된 촬영만으로, 마케터가 최소 시간으로 네이버 블로그(SEO), 인스타그램 캐러셀(포트폴리오), 숏폼 영상(알고리즘)을 동시 생산하는 시스템

### 목표 (Goals)
- **효율성**: 현장 촬영 시간 2분 이내, 마케터 가공 시간 10분 이내
- **일관성**: 100개 작업(100W100C) 달성 시 지역 검색 결과 상위 노출
- **확장성**: 템플릿 기반으로 신규 작업자도 즉시 적용 가능
- **전환율**: 네이버 플레이스 연동으로 검색→문의 전환율 향상

---

## 2. 대상 사용자 (Target Users)

### Primary Users
1. **현장 기술자** (작업자)
   - 휠 복원 작업 수행 후 촬영 담당
   - IT 숙련도: 낮음 (스마트폰 사진 촬영만 가능)
   - 니즈: 추가 업무 부담 없이 간단한 촬영만 수행

2. **마케터** (관리자)
   - 촬영된 사진으로 3개 플랫폼 콘텐츠 제작
   - IT 숙련도: 중간 (Canva, 블로그 포스팅 가능)
   - 니즈: 최소 시간으로 다수 플랫폼 콘텐츠 생산

### Secondary Users
3. **업체 대표**
   - 마케팅 효과 모니터링 (조회수, 문의 건수)
   - 니즈: ROI 측정 및 지역 검색 순위 확인

---

## 3. 핵심 기능 (Core Features)

### Feature 1: 5-Category 촬영 가이드 시스템
**설명**: 현장 기술자용 표준 촬영 체크리스트

**기능 세부사항**:
- **카테고리 정의**:
  1. **입고 (Before-Car)**: 작업 전 차량 전체 (2~3장)
  2. **문제 (Before-Wheel)**: 손상된 휠 클로즈업 (2~3장)
  3. **과정 (During)**: 작업 중 모습 (마스킹/샌딩/도색) (2~3장)
  4. **해결 (After-Wheel)**: 복원 완료 휠 클로즈업 (2~3장)
  5. **출고 (After-Car)**: 작업 후 차량 전체 (2~3장)

- **촬영 환경**: 공장 내 조명 일정한 '포토존' 지정
- **저장 방식**: 공유 폴더에 `YYYY-MM-DD_차종_작업번호` 폴더로 업로드
  - 예: `2025-01-12_G80_WHL001/`

**우선순위**: P0 (필수)

---

### Feature 2: 3-in-1 콘텐츠 템플릿 자동화

#### Sub-Feature 2.1: 네이버 블로그 텍스트 템플릿
**목적**: 지역 SEO 최적화 케이스 스터디

**템플릿 구조**:
```markdown
제목: [지역명+차종] 휠 긁힘 복원, [작업명]으로 3시간 만에 새 휠 변신!

본문:
1. 입고 당시 차량 모습 → [Before-Car 삽입]
2. 휠 손상 상태 확인 → [Before-Wheel 삽입]
3. 꼼꼼한 작업 과정 → [During 삽입]
4. 완벽하게 복원된 휠 → [After-Wheel 삽입] + B/A 비교
5. 출고 준비 완료 → [After-Car 삽입]

CTA: 네이버 지도, 전화번호, 해시태그 3개
```

**자동화 방식**:
- 텍스트 파일로 템플릿 제공
- 마케터가 복사-붙여넣기 후 사진 삽입 (5분 소요)

**우선순위**: P0

---

#### Sub-Feature 2.2: 인스타그램 캐러셀 디자인 템플릿
**목적**: 비주얼 포트폴리오 제작

**템플릿 구성** (10페이지):
1. 커버: Before vs After 비교
2. 입고 사진 + "G80 휠복원 입고"
3-4. Before-Wheel 사진 2장 + "주차 중 발생한 깊은 상처"
5-6. During 사진 2장 + "꼼꼼한 마스킹" / "정품 도료 도색"
7-8. After-Wheel 사진 2장 + "새 휠처럼 완벽 복원"
9. After-Car 사진 + "빛나는 모습으로 출고!"
10. CTA: 로고 + "OO동 휠복원 문의는 프로필 링크"

**자동화 방식**:
- Canva/미리캔버스 템플릿 파일 제공
- 드래그 앤 드롭으로 사진 교체 (3분 소요)

**우선순위**: P0

---

#### Sub-Feature 2.3: 숏폼 포토 슬라이드 템플릿
**목적**: 알고리즘 공략용 릴스/쇼츠

**템플릿 구성** (5초):
- 0:01 - Before-Wheel + 자막 "긁히고"
- 0:02 - During + 자막 "까이고"
- 0:03 - 전환 효과
- 0:04 - After-Wheel + 자막 "완벽 복원!"
- 0:05 - After-Car + 자막 "OO동 휠복원"

**자동화 방식**:
- 인스타그램 릴스 템플릿 또는 CapCut 템플릿
- 사진 4장 선택 후 템플릿 적용 (2분 소요)

**우선순위**: P1 (권장)

---

### Feature 3: 네이버 플레이스 연동 시스템
**설명**: 모든 콘텐츠를 네이버 지도로 수렴

**기능**:
- 블로그 발행 시 → 네이버 플레이스 '새소식' 자동 연동
- 인스타그램 프로필 링크 → 네이버 플레이스 URL
- 숏폼 캡션 → "[지역명] 휠복원" 검색 유도

**우선순위**: P0

---

## 4. 사용자 스토리 (User Stories)

### Story 1: 현장 기술자의 촬영
**AS A** 현장 기술자
**I WANT TO** 5가지 카테고리만 촬영하고 공유 폴더에 업로드
**SO THAT** 추가 마케팅 업무 부담 없이 작업에만 집중할 수 있다

**Tasks**:
- [ ] 포토존 조명 설정
- [ ] 촬영 체크리스트 프린트 배포
- [ ] 공유 폴더 접근 권한 설정
- [ ] 폴더 명명 규칙 교육

---

### Story 2: 마케터의 콘텐츠 생산
**AS A** 마케터
**I WANT TO** 공유 폴더의 사진을 템플릿에 적용하여 10분 내 3개 콘텐츠 완성
**SO THAT** 일일 다수 작업 사례를 빠르게 발행할 수 있다

**Tasks**:
- [ ] 블로그 텍스트 템플릿 파일 생성
- [ ] Canva 캐러셀 템플릿 디자인
- [ ] CapCut 숏폼 템플릿 저장
- [ ] 발행 워크플로우 문서화

---

### Story 3: 대표의 성과 모니터링
**AS A** 업체 대표
**I WANT TO** 주간 콘텐츠 발행 건수와 네이버 검색 순위 확인
**SO THAT** 마케팅 ROI를 측정하고 개선 방향을 결정할 수 있다

**Tasks**:
- [ ] Google Sheets 대시보드 생성 (발행 건수 트래킹)
- [ ] 네이버 검색 순위 주간 모니터링 프로세스

---

## 5. 수락 기준 (Acceptance Criteria)

### 기능 검증
- [ ] **촬영 시간**: 현장 기술자가 5-Category 사진 촬영에 2분 이내 소요
- [ ] **가공 시간**: 마케터가 3개 콘텐츠 제작에 10분 이내 소요 (블로그 5분 + 캐러셀 3분 + 숏폼 2분)
- [ ] **템플릿 완성도**: 신규 마케터가 교육 없이 템플릿만으로 콘텐츠 제작 가능
- [ ] **폴더 규칙 준수율**: 90% 이상의 작업이 올바른 폴더 명명 규칙 사용

### 성과 지표
- [ ] **100W100C 달성**: 100개 작업 사례 누적 시 "[지역명]+휠복원" 검색 결과 1페이지 노출
- [ ] **콘텐츠 일관성**: 블로그, 인스타, 숏폼 모두 동일한 작업 사례 사용
- [ ] **전환율**: 네이버 플레이스 문의 건수 월간 30% 증가 (3개월 내)

### 기술 요구사항
- [ ] 공유 폴더 접근: Google Drive 또는 네트워크 드라이브
- [ ] 템플릿 포맷:
  - 블로그: `.txt` 또는 `.md` 파일
  - 캐러셀: Canva `.json` 템플릿 또는 미리캔버스 링크
  - 숏폼: CapCut 템플릿 링크 또는 릴스 저장된 템플릿

---

## 6. 범위 (Scope)

### In Scope (포함)
✅ 5-Category 촬영 가이드 문서 및 체크리스트
✅ 네이버 블로그 텍스트 템플릿 파일
✅ 인스타그램 캐러셀 Canva 템플릿 디자인
✅ 숏폼 포토 슬라이드 템플릿 (CapCut/릴스)
✅ 폴더 명명 규칙 및 파일 저장 프로세스
✅ 네이버 플레이스 연동 매뉴얼
✅ 마케터용 10분 워크플로우 SOP 문서

### Out of Scope (제외)
❌ 실시간 영상 촬영 자동화 (포토 슬라이드로 대체)
❌ AI 기반 자동 텍스트 생성 (v2.0에서 고려)
❌ 고객 CRM 시스템 연동
❌ 결제/예약 시스템
❌ 다국어 지원 (한국어만)
❌ 사진 품질 자동 검증 (v1.0에서는 수동)

### Future Considerations (향후 고려사항)
- **Phase 2**: AI 기반 사진 품질 자동 검증 (흐릿함/조명 부족 감지)
- **Phase 3**: GPT API 연동 블로그 텍스트 자동 생성
- **Phase 4**: 통합 대시보드 (조회수/문의 건수/검색 순위 실시간 모니터링)

---

## 7. 데이터 및 스키마 (Data & Schema)

### 폴더 구조
```
shared-drive/
├── 2025-01-12_G80_WHL001/
│   ├── 01_before_car_001.jpg
│   ├── 01_before_car_002.jpg
│   ├── 02_before_wheel_001.jpg
│   ├── 02_before_wheel_002.jpg
│   ├── 03_during_001.jpg
│   ├── 03_during_002.jpg
│   ├── 04_after_wheel_001.jpg
│   ├── 04_after_wheel_002.jpg
│   ├── 05_after_car_001.jpg
│   └── 05_after_car_002.jpg
└── 2025-01-13_SantaFe_WHL002/
    └── ...
```

### 파일 명명 규칙
- **폴더명**: `YYYY-MM-DD_차종_작업번호`
- **파일명**: `카테고리번호_카테고리명_순번.jpg`
  - 예: `02_before_wheel_001.jpg`

### 메타데이터 (선택)
```json
{
  "작업번호": "WHL001",
  "작업일": "2025-01-12",
  "차종": "Genesis G80",
  "작업구분": "휠 긁힘 복원",
  "지역": "강남구",
  "촬영자": "김기술",
  "사진수": 10
}
```
*※ v1.0에서는 폴더명으로 대체, v2.0에서 JSON 파일 추가 고려*

---

## 8. 기술 스택 및 디자인 (Technical & Design)

### 기술 스택
| 레이어 | 도구/플랫폼 | 용도 |
|--------|------------|------|
| 촬영 | 스마트폰 카메라 | 현장 사진 촬영 |
| 저장소 | Google Drive / Network Drive | 공유 폴더 |
| 블로그 | 네이버 블로그 | SEO 콘텐츠 발행 |
| 캐러셀 | Canva / 미리캔버스 | 인스타그램 디자인 |
| 숏폼 | CapCut / 인스타 릴스 템플릿 | 숏폼 영상 제작 |
| 연동 | 네이버 플레이스 | 통합 마케팅 허브 |

### 디자인 가이드라인
- **색상**: 브랜드 컬러 (예: 블루 계열)
- **폰트**: 고딕 계열 (가독성 우선)
- **로고**: 모든 콘텐츠 우측 하단 워터마크
- **자막**: 가독성 높은 흰색 테두리 검은색 텍스트

### 보안 및 개인정보
- **차량 번호판 모자이크**: Before-Car, After-Car 사진에서 자동 또는 수동 처리 (v1.0: 수동, v2.0: AI 자동)
- **고객 정보 제외**: 블로그/SNS에 고객 성명/연락처 미포함
- **공유 폴더 권한**: 현장 기술자/마케터/대표만 접근 가능

---

## 9. 개발 Phase (단계별 구현 로드맵)

> **핵심 원칙**: 무료 오픈소스 기반 | 스마트폰 최우선 | 단계별 안전한 확장

---

### 📋 docs/prd.md와의 차이점

이 PRD는 초기 개념 문서([docs/prd.md](../../docs/prd.md))에서 기술 스택이 변경되었습니다.

| 항목 | 개념 문서 | 이 문서 (실제 구현) | 변경 이유 |
|------|----------|---------------------|----------|
| **저장소** | Google Drive | **Imgur API** | 무료 무제한, CDN 속도 |
| **DB** | 미정의 | **Supabase PostgreSQL** | SQL 쿼리, RLS 보안 |
| **인증** | 미정의 | **Supabase Auth** | Google OAuth 내장 |
| **템플릿** | Canva (수동) | **Fabric.js (자동)** | 프로그래밍 가능 |
| **서버리스** | 미정의 | **Supabase Edge Functions** | Deno 기반, 월 50만 호출 무료 |

**결론**: 개념 문서는 참고용이며, 개발 시 아래 Phase 0-5의 기술 스택을 따르세요.

---

### **Phase 0 (MVP): 기반 인프라 구축** (1~2주, P0 필수)
**목표**: 스마트폰에서 사진 촬영 → Imgur 업로드 → Supabase에 폴더별 메타데이터 저장

> **MVP 핵심**: 최소한의 기능으로 "촬영 → 저장 → 조회" 검증

---

#### 기술 스택 (100% 무료)

| 항목 | 기술 | 무료 한도 | 선택 이유 |
|------|------|-----------|----------|
| **Frontend** | PWA + HTML5 | 무제한 | 앱 설치 불필요, 크로스 플랫폼 |
| **UI 프레임워크** | Bootstrap 5 | 무제한 | CDN 제공, 반응형 즉시 구현 |
| **인증** | Supabase Auth | 월 5만 사용자 | Google OAuth 2.0 기본 제공 |
| **데이터베이스** | Supabase PostgreSQL | 500MB | 관계형 DB, SQL 쿼리 가능 |
| **이미지 저장소** | Imgur API | 무제한 업로드 | 익명 업로드 가능, CDN 무료 |
| **호스팅** | Vercel / Netlify | 100GB/월 | GitHub 연동, 자동 배포 |

**왜 Supabase + Imgur인가?**
- ✅ **Supabase**: Firebase 대비 SQL 쿼리 강력, REST API 자동 생성, Row Level Security
- ✅ **Imgur**: 이미지 전용 CDN, 용량 제한 없음, 썸네일 자동 생성
- ✅ **분리 아키텍처**: 메타데이터(Supabase) ↔ 이미지(Imgur) 분리로 확장성 우수
- ✅ **완전 무료**: Firebase Storage 5GB 제한 없음, 프로덕션 준비 완료

---

#### 인프라 아키텍처

```
[스마트폰]
    ↓ (촬영)
[PWA 웹앱]
    ↓ (Google 로그인)
[Supabase Auth] ← 사용자 인증
    ↓
[이미지 업로드]
    ├→ [Imgur API] ← 이미지 저장 + URL 반환
    └→ [Supabase DB] ← 작업정보 + Imgur URL 저장
         (jobs 테이블: 작업번호, 날짜, 차종, 카테고리별 URL)
```

---

#### 데이터베이스 스키마 (Supabase PostgreSQL)

##### 1. users 테이블 (Supabase Auth 기본 제공)
```sql
-- Supabase가 자동 생성
auth.users (
  id UUID PRIMARY KEY,
  email TEXT,
  provider TEXT, -- 'google'
  created_at TIMESTAMP
)
```

##### 2. jobs 테이블 (작업 관리)
```sql
CREATE TABLE jobs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  job_number TEXT UNIQUE NOT NULL,          -- 'WHL001', 'WHL002'...
  created_at TIMESTAMP DEFAULT NOW(),
  work_date DATE NOT NULL,                  -- 작업일
  car_model TEXT NOT NULL,                  -- '제네시스 G80'
  work_type TEXT DEFAULT '휠 복원',         -- 작업 유형
  technician_id UUID REFERENCES auth.users(id),
  status TEXT DEFAULT 'uploaded',           -- 'uploaded', 'processing', 'published'

  -- 메타데이터
  location TEXT,                            -- '강남구'
  notes TEXT,

  -- 타임스탬프
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 인덱스 생성 (빠른 조회)
CREATE INDEX idx_jobs_date ON jobs(work_date DESC);
CREATE INDEX idx_jobs_technician ON jobs(technician_id);
CREATE INDEX idx_jobs_status ON jobs(status);
```

##### 3. photos 테이블 (사진 메타데이터)
```sql
CREATE TABLE photos (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  job_id UUID REFERENCES jobs(id) ON DELETE CASCADE,
  category TEXT NOT NULL,                   -- 'before_car', 'before_wheel', 'during', 'after_wheel', 'after_car'
  imgur_url TEXT NOT NULL,                  -- 'https://i.imgur.com/abc123.jpg'
  imgur_delete_hash TEXT,                   -- Imgur 삭제용 해시 (익명 업로드 시)
  thumbnail_url TEXT,                       -- Imgur 썸네일 (중간 크기)
  file_size INTEGER,                        -- 파일 크기 (bytes)
  uploaded_at TIMESTAMP DEFAULT NOW(),
  sequence INTEGER DEFAULT 1                -- 카테고리 내 순서 (1, 2, 3...)
);

-- 인덱스
CREATE INDEX idx_photos_job ON photos(job_id);
CREATE INDEX idx_photos_category ON photos(category);
```

##### 4. Row Level Security (RLS) 설정
```sql
-- jobs 테이블: 본인 작업만 조회 가능
ALTER TABLE jobs ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own jobs"
  ON jobs FOR SELECT
  USING (auth.uid() = technician_id);

CREATE POLICY "Users can insert own jobs"
  ON jobs FOR INSERT
  WITH CHECK (auth.uid() = technician_id);

-- photos 테이블: 본인 작업의 사진만 조회 가능
ALTER TABLE photos ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own photos"
  ON photos FOR SELECT
  USING (
    job_id IN (
      SELECT id FROM jobs WHERE technician_id = auth.uid()
    )
  );
```

---

#### 폴더 구조 (프로젝트)

```
photo-factory-mvp/
├── public/
│   ├── index.html              # 로그인 페이지
│   ├── upload.html             # 촬영/업로드 페이지
│   ├── gallery.html            # 작업 목록 조회
│   ├── manifest.json           # PWA 설정
│   └── service-worker.js       # 오프라인 지원 (선택)
├── js/
│   ├── config.js               # Supabase + Imgur API 키
│   ├── auth.js                 # Supabase Auth 로직
│   ├── upload.js               # Imgur 업로드 + Supabase 저장
│   └── gallery.js              # 작업 목록 조회
├── css/
│   └── styles.css              # 커스텀 스타일
├── .env                        # 환경변수 (로컬 개발용)
├── .gitignore
├── package.json                # (선택) 개발 도구용
└── README.md
```

---

#### 구현 체크리스트

##### Step 1: Supabase 프로젝트 설정 (15분)
- [ ] [Supabase](https://supabase.com) 가입 및 프로젝트 생성
- [ ] **Authentication** 설정:
  - [ ] Google Provider 활성화
  - [ ] Redirect URL 설정 (`http://localhost:8080`, `https://your-app.vercel.app`)
- [ ] **Database** 설정:
  - [ ] `jobs` 테이블 생성 (SQL Editor에서 위 스키마 실행)
  - [ ] `photos` 테이블 생성
  - [ ] RLS 정책 활성화
- [ ] **API Keys** 복사:
  - [ ] `SUPABASE_URL`
  - [ ] `SUPABASE_ANON_KEY` (공개 키)

##### Step 2: Imgur API 설정 (5분)
- [ ] [Imgur API](https://api.imgur.com/oauth2/addclient) 가입
- [ ] **Anonymous Upload** 용 Client ID 발급
- [ ] `IMGUR_CLIENT_ID` 복사

##### Step 3: 로컬 개발 환경 (10분)
- [ ] 프로젝트 폴더 생성 및 구조 초기화
- [ ] `.env` 파일 생성:
  ```env
  SUPABASE_URL=https://your-project.supabase.co
  SUPABASE_ANON_KEY=your_anon_key
  IMGUR_CLIENT_ID=your_imgur_client_id
  ```
- [ ] `js/config.js` 생성:
  ```javascript
  export const SUPABASE_URL = 'https://your-project.supabase.co';
  export const SUPABASE_ANON_KEY = 'your_anon_key';
  export const IMGUR_CLIENT_ID = 'your_imgur_client_id';
  ```

##### Step 4: 인증 구현 (`auth.js`) (30분)
- [ ] Supabase Client 초기화
- [ ] Google OAuth 로그인 버튼 구현
- [ ] 로그인 상태 확인 및 리다이렉트
- [ ] 로그아웃 기능

##### Step 5: 업로드 구현 (`upload.js`) (1시간)
- [ ] **UI 구성**:
  - [ ] 5개 카테고리 탭 (Bootstrap Nav Tabs)
  - [ ] 각 탭마다 카메라 버튼 (`<input type="file" accept="image/*" capture="camera">`)
  - [ ] 작업번호 자동 생성 (형식: `WHL` + 날짜 + 시퀀스)
  - [ ] 차종 입력 필드
- [ ] **Imgur 업로드 로직**:
  ```javascript
  async function uploadToImgur(file) {
    const formData = new FormData();
    formData.append('image', file);

    const response = await fetch('https://api.imgur.com/3/image', {
      method: 'POST',
      headers: {
        'Authorization': `Client-ID ${IMGUR_CLIENT_ID}`
      },
      body: formData
    });

    const data = await response.json();
    return {
      url: data.data.link,           // 원본 URL
      deleteHash: data.data.deletehash,
      thumbnail: data.data.link.replace('.jpg', 'm.jpg') // 중간 크기
    };
  }
  ```
- [ ] **Supabase 저장 로직**:
  1. `jobs` 테이블에 작업 정보 INSERT
  2. `photos` 테이블에 Imgur URL INSERT
- [ ] **에러 처리**: 네트워크 실패, Imgur 할당량 초과 등

##### Step 6: 갤러리 구현 (`gallery.js`) (1시간)
- [ ] Supabase에서 `jobs` 목록 조회 (최신순)
- [ ] 각 작업의 썸네일 표시 (첫 번째 사진)
- [ ] 작업 클릭 → 5개 카테고리별 사진 그리드 표시
- [ ] Lightbox 라이브러리로 사진 확대 보기

##### Step 7: 배포 (Vercel) (15분)
- [ ] GitHub 저장소 생성 및 푸시
- [ ] Vercel 가입 및 프로젝트 연동
- [ ] 환경변수 설정 (Vercel Dashboard)
- [ ] 자동 배포 확인

---

#### 산출물

##### 1. 로그인 페이지 (`index.html`)
```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>포토 팩토리 - 로그인</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container vh-100 d-flex align-items-center justify-content-center">
    <div class="card shadow-sm" style="max-width: 400px; width: 100%;">
      <div class="card-body text-center p-5">
        <h2 class="mb-4">5-Category 포토 팩토리</h2>
        <p class="text-muted mb-4">현장 촬영 2분, 마케터 가공 10분</p>
        <button id="loginBtn" class="btn btn-primary btn-lg w-100">
          <i class="bi bi-google"></i> Google로 시작하기
        </button>
      </div>
    </div>
  </div>

  <script type="module">
    import { supabase } from './js/auth.js';

    document.getElementById('loginBtn').addEventListener('click', async () => {
      const { error } = await supabase.auth.signInWithOAuth({
        provider: 'google',
        options: {
          redirectTo: window.location.origin + '/upload.html'
        }
      });
      if (error) alert('로그인 실패: ' + error.message);
    });
  </script>
</body>
</html>
```

##### 2. 업로드 페이지 UI (핵심 부분)
```html
<!-- 5개 카테고리 탭 -->
<ul class="nav nav-tabs" id="categoryTabs">
  <li class="nav-item">
    <a class="nav-link active" data-category="before_car">입고</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" data-category="before_wheel">문제</a>
  </li>
  <!-- ... 나머지 탭 -->
</ul>

<!-- 카메라 버튼 -->
<input type="file" id="cameraInput" accept="image/*" capture="camera" style="display:none">
<button class="btn btn-primary btn-lg" onclick="document.getElementById('cameraInput').click()">
  📷 사진 촬영
</button>

<!-- 작업 정보 입력 -->
<input type="text" id="carModel" placeholder="차종 (예: G80)" class="form-control">
<button id="submitBtn" class="btn btn-success btn-lg">업로드 완료</button>
```

---

#### 수락 기준 (MVP)

##### 기능 검증
- [ ] **로그인**: 스마트폰에서 Google 계정으로 로그인 (30초 이내)
- [ ] **촬영**: 5개 카테고리에 각각 2장씩 촬영 (총 10장, 2분 이내)
- [ ] **업로드**:
  - [ ] 10장 사진이 Imgur에 업로드 완료
  - [ ] Supabase `jobs` 테이블에 작업 1건 생성
  - [ ] Supabase `photos` 테이블에 사진 10건 생성
- [ ] **조회**:
  - [ ] 갤러리 페이지에서 업로드한 작업 확인
  - [ ] 카테고리별 사진 분류 표시 (5개 탭)
  - [ ] 사진 클릭 시 원본 크기 보기

##### 성능 기준
- [ ] 사진 1장 업로드: 3초 이내 (4G 네트워크 기준)
- [ ] 갤러리 목록 로딩: 1초 이내 (작업 100건 기준)
- [ ] 모바일 반응형: iPhone SE ~ Galaxy S22 정상 작동

##### 보안 기준
- [ ] RLS 적용: 다른 사용자의 작업 조회 불가능
- [ ] API 키 노출 방지: `.env` 파일 `.gitignore` 등록
- [ ] HTTPS 배포: Vercel 자동 인증서

---

#### 예상 소요 시간

| 작업 | 시간 | 담당 |
|------|------|------|
| Supabase 설정 | 15분 | 개발자 |
| Imgur API 설정 | 5분 | 개발자 |
| 인증 구현 | 30분 | 개발자 |
| 업로드 구현 | 1시간 | 개발자 |
| 갤러리 구현 | 1시간 | 개발자 |
| 테스트 및 디버깅 | 1시간 | 개발자 |
| Vercel 배포 | 15분 | 개발자 |
| **총 소요 시간** | **4시간 30분** | - |

**실무 기준**: 1일 (디자인 조정, 예외 처리 포함) ~ 2일 (초급 개발자)

---

### **Phase 1: 마케터 관리 시스템** (1주, P0 필수)
**목표**: 마케터가 PC에서 업로드된 사진을 카테고리별로 조회 및 다운로드

#### 신규 기능
- [ ] **마케터 대시보드 페이지** (`dashboard.html`)
  - Supabase에서 작업 목록 조회 (REST API)
  - 작업별 썸네일 그리드 (Imgur 썸네일 URL 사용)
  - 필터: 날짜, 차종, 작업번호, 상태
- [ ] **작업 상세 페이지** (`job-detail.html`)
  - 5개 카테고리 탭으로 사진 분류 표시
  - 사진 선택 → ZIP 다운로드 (JSZip)
  - 사진 개별 다운로드/삭제 기능 (Imgur delete_hash 사용)
- [ ] **권한 관리**:
  - 마케터 역할: 모든 작업 조회 가능
  - 기술자 역할: 본인 작업만 조회 (RLS 적용)

#### 기술 추가
| 항목 | 기술 | 용도 |
|------|------|------|
| 사진 압축 | JSZip (무료) | 선택 사진 ZIP 다운로드 |
| 이미지 뷰어 | Lightbox2 (무료) | 사진 확대 보기 |
| 데이터 페칭 | Supabase JS Client | REST API 자동 생성 |

#### Supabase 쿼리 예시
```javascript
// 최신 작업 10건 조회
const { data: jobs } = await supabase
  .from('jobs')
  .select(`
    *,
    photos (
      id, category, imgur_url, thumbnail_url, sequence
    )
  `)
  .order('work_date', { ascending: false })
  .limit(10);

// 특정 작업의 카테고리별 사진 조회
const { data: photos } = await supabase
  .from('photos')
  .select('*')
  .eq('job_id', jobId)
  .order('category')
  .order('sequence');
```

#### 수락 기준
- [ ] 마케터가 PC에서 모든 작업 목록 확인 가능 (페이지네이션: 20건/페이지)
- [ ] 작업 클릭 시 5개 카테고리별 사진 분류 표시
- [ ] 선택한 사진들을 ZIP으로 일괄 다운로드 (10장 기준 5초 이내)
- [ ] 사진 삭제 시 Imgur + Supabase 동시 삭제

---

### **Phase 2: 템플릿 생성 도구** (1주, P0 필수)
**목표**: 마케터가 사진을 템플릿에 적용하여 블로그/인스타 콘텐츠 생성

#### 신규 기능
- [ ] **블로그 템플릿 에디터**:
  - 미리 작성된 Markdown 템플릿 불러오기
  - 사진 드래그 앤 드롭으로 삽입
  - 키워드 자동 치환 (`[지역명]`, `[차종]`)
  - Markdown → HTML 변환 후 복사 버튼
- [ ] **인스타 캐러셀 생성기**:
  - Fabric.js로 10페이지 캔버스 생성
  - 사진 드래그 앤 드롭 + 텍스트 자막 입력
  - 각 페이지 PNG 다운로드 (1080x1080)

#### 기술 추가
| 항목 | 기술 | 용도 |
|------|------|------|
| Markdown 에디터 | SimpleMDE (무료) | 블로그 텍스트 편집 |
| 캔버스 편집 | Fabric.js (무료) | 인스타 캐러셀 디자인 |
| Markdown → HTML | Marked.js (무료) | 변환 라이브러리 |

#### 템플릿 저장소 (Supabase PostgreSQL)

##### 테이블 스키마
```sql
CREATE TABLE templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  template_id TEXT UNIQUE NOT NULL,       -- 'blog_basic', 'instagram_standard'
  type TEXT NOT NULL,                     -- 'blog', 'instagram', 'shortform'
  name TEXT NOT NULL,                     -- '기본 블로그 템플릿'
  content TEXT NOT NULL,                  -- Markdown 또는 HTML 템플릿
  placeholders TEXT[],                    -- ['지역명', '차종', '작업명']
  preview_image TEXT,                     -- 미리보기 이미지 URL
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 기본 템플릿 삽입
INSERT INTO templates (template_id, type, name, content, placeholders) VALUES
('blog_basic', 'blog', '기본 블로그 템플릿',
'# [지역명+차종] 휠 긁힘 복원, [작업명]으로 3시간 만에 새 휠 변신!

안녕하세요. [지역명] 휠복원 전문 [업체명]입니다.
오늘은 [차종] 오너께서 휠 상처 때문에 방문 주셨습니다.

## 1. 입고 당시 차량 모습
{{PHOTO:before_car}}

## 2. 휠 손상 상태 확인
{{PHOTO:before_wheel}}

## 3. 꼼꼼한 작업 과정
{{PHOTO:during}}

## 4. 완벽하게 복원된 휠
{{PHOTO:after_wheel}}

## 5. 출고 준비 완료!
{{PHOTO:after_car}}

고객님께서 새 휠 같다며 매우 만족하셨습니다.
[지역명]에서 휠 복원이 필요할 땐 언제든 문의주세요.

📍 [네이버 지도 링크]
☎️ [전화번호]
#[지역명]휠복원 #[차종]휠복원 #[작업명]',
ARRAY['지역명', '차종', '작업명', '업체명']);
```

##### JavaScript 조회 예시
```javascript
// Supabase에서 템플릿 목록 조회
const { data: templates } = await supabase
  .from('templates')
  .select('*')
  .eq('type', 'blog')
  .order('created_at', { ascending: false });

// 특정 템플릿 가져오기
const { data: template } = await supabase
  .from('templates')
  .select('*')
  .eq('template_id', 'blog_basic')
  .single();

// 키워드 치환 함수
function applyTemplate(template, variables) {
  let content = template.content;

  // 텍스트 치환
  template.placeholders.forEach(placeholder => {
    const regex = new RegExp(`\\[${placeholder}\\]`, 'g');
    content = content.replace(regex, variables[placeholder] || '');
  });

  // 사진 치환
  content = content.replace(/\{\{PHOTO:(\w+)\}\}/g, (match, category) => {
    return `![${category}](${variables.photos[category]})`;
  });

  return content;
}
```

#### 수락 기준
- [ ] 블로그 템플릿에 사진 5장 삽입 후 HTML 복사 (3분 이내)
- [ ] 인스타 캐러셀 10페이지 생성 후 PNG 다운로드 (2분 이내)
- [ ] 키워드 자동 치환 정확도 100%

---

### **Phase 3: 자동화 발행 시스템** (2주, P1 권장)
**목표**: 생성된 콘텐츠를 네이버 블로그에 자동 발행

#### 신규 기능
- [ ] **네이버 블로그 API 연동**:
  - OAuth 2.0 인증 (네이버 개발자 센터)
  - 블로그 게시글 자동 발행 API 호출
  - 발행 상태 Supabase DB에 저장
- [ ] **예약 발행 시스템**:
  - Supabase Edge Functions (Deno 기반, 무료)
  - Supabase Cron Jobs (pg_cron)으로 지정 시간에 자동 발행
  - 또는 클라이언트 측에서 setTimeout/Interval 사용

#### 기술 추가
| 항목 | 기술 | 용도 | 무료 한도 |
|------|------|------|----------|
| 서버리스 함수 | Supabase Edge Functions | 백엔드 로직 (Deno Runtime) | 월 50만 호출 |
| 예약 작업 | Supabase pg_cron | 스케줄 작업 | 무제한 |
| API 요청 | Fetch API (기본 제공) | HTTP 클라이언트 | - |

#### Supabase Edge Function 구조

##### 1. 함수 생성 (`supabase/functions/publish-blog/index.ts`)
```typescript
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

serve(async (req) => {
  const { jobId, blogContent } = await req.json()

  // Supabase Client 초기화
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? ''
  )

  // 네이버 블로그 API 호출
  const naverResponse = await fetch('https://openapi.naver.com/blog/writePost.json', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${Deno.env.get('NAVER_ACCESS_TOKEN')}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      title: blogContent.title,
      contents: blogContent.body
    })
  })

  const result = await naverResponse.json()

  // 발행 상태 Supabase에 저장
  await supabase
    .from('jobs')
    .update({
      status: 'published',
      published_at: new Date().toISOString(),
      blog_url: result.post_url
    })
    .eq('id', jobId)

  return new Response(JSON.stringify({ success: true, url: result.post_url }), {
    headers: { 'Content-Type': 'application/json' }
  })
})
```

##### 2. 배포 명령
```bash
# Edge Function 배포
supabase functions deploy publish-blog

# 환경변수 설정
supabase secrets set NAVER_CLIENT_ID=your_client_id
supabase secrets set NAVER_CLIENT_SECRET=your_secret
supabase secrets set NAVER_ACCESS_TOKEN=your_token
```

##### 3. 클라이언트에서 호출
```javascript
// 즉시 발행
const { data, error } = await supabase.functions.invoke('publish-blog', {
  body: {
    jobId: 'uuid-here',
    blogContent: {
      title: '제네시스 G80 휠 복원',
      body: '<html>...</html>'
    }
  }
})
```

#### 예약 발행 구현 (선택지 2가지)

##### 옵션 1: Supabase pg_cron (서버 측, 권장)
```sql
-- pg_cron 확장 활성화 (Supabase Dashboard > Database > Extensions)
CREATE EXTENSION IF NOT EXISTS pg_cron;

-- 매일 오전 9시에 예약된 작업 발행
SELECT cron.schedule(
  'publish-scheduled-jobs',
  '0 9 * * *', -- 매일 오전 9시
  $$
  SELECT net.http_post(
    url := 'https://your-project.supabase.co/functions/v1/publish-blog',
    headers := '{"Authorization": "Bearer YOUR_ANON_KEY"}'::jsonb,
    body := json_build_object('jobId', id, 'scheduled', true)::jsonb
  )
  FROM jobs
  WHERE status = 'scheduled' AND scheduled_time <= NOW();
  $$
);
```

##### 옵션 2: 클라이언트 측 (간단, MVP 권장)
```javascript
// 마케터 대시보드에서 예약 설정
async function schedulePublish(jobId, publishTime) {
  // Supabase에 예약 시간 저장
  await supabase
    .from('jobs')
    .update({
      status: 'scheduled',
      scheduled_time: publishTime
    })
    .eq('id', jobId)

  // 클라이언트에서 주기적으로 확인 (5분마다)
  setInterval(async () => {
    const { data: scheduledJobs } = await supabase
      .from('jobs')
      .select('*')
      .eq('status', 'scheduled')
      .lte('scheduled_time', new Date().toISOString())

    scheduledJobs?.forEach(job => {
      supabase.functions.invoke('publish-blog', { body: { jobId: job.id } })
    })
  }, 5 * 60 * 1000) // 5분
}
```

#### 네이버 API 설정
```bash
# Supabase Secrets로 관리 (안전)
supabase secrets set NAVER_CLIENT_ID=your_client_id
supabase secrets set NAVER_CLIENT_SECRET=your_secret
supabase secrets set NAVER_BLOG_ID=your_blog_id

# 로컬 개발 시 (.env)
NAVER_CLIENT_ID=your_client_id
NAVER_CLIENT_SECRET=your_secret
NAVER_BLOG_ID=your_blog_id
```

#### 발행 상태 추적 (Supabase DB)
```sql
-- jobs 테이블에 컬럼 추가
ALTER TABLE jobs
ADD COLUMN published_at TIMESTAMP,
ADD COLUMN blog_url TEXT,
ADD COLUMN scheduled_time TIMESTAMP;

-- 상태 업데이트 예시
UPDATE jobs
SET status = 'published',
    published_at = NOW(),
    blog_url = 'https://blog.naver.com/...'
WHERE id = 'uuid-here';
```

#### 수락 기준
- [ ] 마케터가 "발행" 버튼 클릭 시 네이버 블로그에 자동 게시
- [ ] 예약 발행 설정 시 지정 시간에 자동 발행 (오차 1분 이내)
- [ ] 발행 실패 시 이메일 알림 발송

---

### **Phase 4: 분석 대시보드** (1주, P2 선택)
**목표**: 업체 대표가 마케팅 성과를 한눈에 확인

#### 신규 기능
- [ ] **통계 대시보드**:
  - 일일/주간/월간 콘텐츠 발행 건수
  - 네이버 블로그 조회수 (Naver Analytics API)
  - 작업 진행 상태 파이 차트
- [ ] **검색 순위 모니터링**:
  - Google Custom Search API로 "[지역명]+휠복원" 순위 체크
  - 주간 순위 변동 그래프

#### 기술 추가
| 항목 | 기술 | 용도 |
|------|------|------|
| 차트 라이브러리 | Chart.js (무료) | 통계 시각화 |
| API | Google Custom Search API (무료 플랜: 일 100회) | 검색 순위 조회 |

#### 수락 기준
- [ ] 대시보드에서 누적 발행 건수 실시간 표시
- [ ] 주간 검색 순위 변동 그래프 확인 가능

---

### **Phase 5: 고도화 및 최적화** (지속)
**목표**: 사용자 피드백 반영 및 성능 개선

- [ ] **사진 품질 자동 검증**: TensorFlow.js로 흐릿함 감지 (무료)
- [ ] **AI 텍스트 생성**: Hugging Face Inference API (무료 플랜) 로 블로그 텍스트 자동 생성
- [ ] **PWA 오프라인 모드**: 현장에서 네트워크 없어도 사진 임시 저장 후 나중에 업로드
- [ ] **알림 시스템**: Firebase Cloud Messaging으로 푸시 알림 (무료)

---

## 전체 Phase 흐름도

```
Phase 0: 스마트폰 로그인 + 사진 업로드 기반 구축
   ↓
Phase 1: 마케터가 PC에서 사진 조회/다운로드
   ↓
Phase 2: 템플릿 기반 콘텐츠 생성 (블로그/인스타)
   ↓
Phase 3: 네이버 블로그 자동 발행
   ↓
Phase 4: 성과 분석 대시보드
   ↓
Phase 5: AI 기반 고도화 (품질 검증, 텍스트 자동 생성)
```

---

## Phase별 예상 일정

| Phase | 기간 | 누적 기간 | 산출물 |
|-------|------|----------|--------|
| Phase 0 | 1~2주 | 2주 | 스마트폰 사진 업로드 웹앱 |
| Phase 1 | 1주 | 3주 | 마케터 대시보드 |
| Phase 2 | 1주 | 4주 | 템플릿 에디터 |
| Phase 3 | 2주 | 6주 | 네이버 자동 발행 |
| Phase 4 | 1주 | 7주 | 분석 대시보드 |
| Phase 5 | 지속 | - | AI 고도화 |

**최소 기능(MVP)**: Phase 0~2 완료 시 수동 운영 가능 (4주)
**완전 자동화**: Phase 3 완료 필요 (6주)

---

## 10. 리스크 및 대응 (Risks & Mitigation)

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|----------|
| 기술자가 촬영 규칙을 지키지 않음 | High | 포토존에 체크리스트 부착, 매주 우수 촬영 사례 공유 |
| 사진 품질 불량 (흐릿함, 조명 부족) | Medium | 포토존 조명 개선, 촬영 후 즉시 확인 프로세스 |
| 템플릿 적용 시간 초과 (10분 이상) | Medium | 워크플로우 재교육, 템플릿 단순화 |
| 네이버 검색 순위 상승 없음 | Low | 키워드 최적화, 백링크 전략 추가 |
| 차량 번호판 노출 개인정보 문제 | High | 촬영 시 각도 조정 또는 발행 전 모자이크 의무화 |

---

## 11. 성공 지표 (Success Metrics)

### 단기 지표 (1개월)
- ✅ 일일 콘텐츠 발행 건수: 평균 3건 이상
- ✅ 마케터 가공 시간: 평균 10분 이내
- ✅ 템플릿 사용률: 90% 이상

### 중기 지표 (3개월)
- ✅ 누적 블로그 게시물: 90개 이상
- ✅ 네이버 "[지역명]+휠복원" 검색 순위: 1페이지 진입
- ✅ 네이버 플레이스 월간 문의: 30% 증가

### 장기 지표 (6개월)
- ✅ 100W100C 달성 (100개 케이스 스터디)
- ✅ 지역 검색 결과 독점 (1페이지 내 자사 콘텐츠 50% 이상)
- ✅ 신규 고객 유입 60% 증가

---

## 12. 참고 자료 (References)

### 관련 문서
- [TOKEN_OPTIMIZATION_DETAILS.md](../../docs/TOKEN_OPTIMIZATION_DETAILS.md)
- [CLAUDE.md](../../CLAUDE.md) - Phase 0 PRD 가이드

### 외부 리소스
- [Canva 템플릿 가이드](https://www.canva.com/templates/)
- [네이버 플레이스 새소식 등록 방법](https://place.naver.com/)
- [CapCut 템플릿 사용법](https://www.capcut.com/)

### 벤치마크
- 유사 업종 SEO 전략 분석: 자동차 디테일링, 카랩핑 업체

---

## 부록: 템플릿 샘플

### A. 블로그 텍스트 템플릿 (샘플)
```markdown
# [지역명+차종] 휠 긁힘 복원, [작업명]으로 3시간 만에 새 휠 변신!

안녕하세요. [지역명] 휠복원 전문 [업체명]입니다.
오늘은 [차종] 오너께서 휠 상처 때문에 방문 주셨습니다.

## 1. 입고 당시 차량 모습
[Before-Car 사진 삽입]

## 2. 휠 손상 상태 확인
[Before-Wheel 사진 삽입]

## 3. [업체명]의 꼼꼼한 작업 과정
[During 사진 삽입]

## 4. 완벽하게 복원된 휠
[After-Wheel 사진 삽입]
[B/A 비교 사진 삽입]

## 5. 출고 준비 완료!
[After-Car 사진 삽입]

고객님께서 새 휠 같다며 매우 만족하셨습니다.
[지역명]에서 휠 복원이 필요할 땐 언제든 문의주세요.

📍 [네이버 지도 링크]
☎️ [전화번호]
#[지역명]휠복원 #[차종]휠복원 #[작업명]
```

---

**PRD 작성 완료**
**Next Steps**: Task List 생성 → `python scripts/generate_tasks.py tasks/prds/0001-prd-5-category-photo-factory.md`